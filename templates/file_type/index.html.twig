{% extends 'base.html.twig' %}

{% block title %}FileType index{% endblock %}

{% block body %}
    <h1>FileType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for file_type in file_types %}
            <tr>
                <td>{{ file_type.id }}</td>
                <td>{{ file_type.name }}</td>
                <td>{{ file_type.description }}</td>
                <td>
                    <a href="{{ path('app_file_type_show', {'id': file_type.id}) }}">show</a>
                    <a href="{{ path('app_file_type_edit', {'id': file_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_file_type_new') }}">Create new</a>
{% endblock %}
