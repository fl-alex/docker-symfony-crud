{% extends 'base.html.twig' %}

{% block title %}FileType{% endblock %}

{% block body %}
    <h1>FileType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ file_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ file_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ file_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_file_type_index') }}">back to list</a>

    <a href="{{ path('app_file_type_edit', {'id': file_type.id}) }}">edit</a>

    {{ include('file_type/_delete_form.html.twig') }}
{% endblock %}
