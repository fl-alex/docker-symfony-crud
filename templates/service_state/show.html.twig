{% extends 'base.html.twig' %}

{% block title %}ServiceState{% endblock %}

{% block body %}
    <h1>ServiceState</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_state.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ service_state.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ service_state.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_service_state_index') }}">back to list</a>

    <a href="{{ path('app_service_state_edit', {'id': service_state.id}) }}">edit</a>

    {{ include('service_state/_delete_form.html.twig') }}
{% endblock %}
