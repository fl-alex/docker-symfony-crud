<form method="post" action="{{ path('app_object_type_delete', {'id': object_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ object_type.id) }}">
    <button class="btn">Delete</button>
</form>
