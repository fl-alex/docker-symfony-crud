{% extends 'base.html.twig' %}

{% block title %}ObjectType{% endblock %}

{% block body %}
    <h1>ObjectType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ object_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ object_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ object_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_object_type_index') }}">back to list</a>

    <a href="{{ path('app_object_type_edit', {'id': object_type.id}) }}">edit</a>

    {{ include('object_type/_delete_form.html.twig') }}
{% endblock %}
