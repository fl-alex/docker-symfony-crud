<form method="post" action="{{ path('app_objectservice_delete', {'id': objectservice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objectservice.id) }}">
    <button class="btn">Delete</button>
</form>
